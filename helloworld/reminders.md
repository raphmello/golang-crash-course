#### Go commands
`go build` : build and creates de compiled file

`go run` : build and run

`go fmt` : format code

`go install` : compiles and install package

`go get` : download source code of someone else's package

`go test` : run tests
